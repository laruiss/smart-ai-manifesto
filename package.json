{
  "name": "smart-ai-manifesto",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev:convex": "npx convex dev",
    "dev:nuxt": "nuxt dev",
    "dev": "npx convex dev --start 'npm run dev:nuxt'",
    "deploy:netlify": "npx convex deploy --cmd 'npm run generate'",
    "generate": "nuxt generate --preset netlify",
    "preview": "nuxt preview"
  },
  "dependencies": {
    "@clerk/nuxt": "^3.0.1",
    "@nuxt/content": "^3.15.2",
    "convex": "^1.43.0",
    "convex-vue": "^0.1.5",
    "nuxt": "^4.5.1",
    "vue": "^3.5.18"
  }
}
